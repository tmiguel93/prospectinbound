# Acesso na rede local

O servidor usa `HOST=0.0.0.0` e `PORT=3000` por padrão. Após iniciar, acesse `http://IP-DO-COMPUTADOR:3000` em outro dispositivo da mesma rede, permitindo a porta no Firewall do Windows se solicitado.
